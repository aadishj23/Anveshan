{
	"name": "anveshan",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start:backend": "cd backend && npm install && cd .. && node backend/index.js",
		"start:frontend": "cd frontend && npm install && npm run dev",
		"start": "npm i && concurrently \"npm run start:backend\" \"npm run start:frontend\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.0.1"
	}
}
